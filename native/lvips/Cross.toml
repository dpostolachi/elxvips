[build.env]
passthrough = [
  "RUSTLER_NIF_VERSION"
]

[target.aarch64-unknown-linux-gnu]
pre-build = ["""
    export DEBIAN_FRONTEND=noninteractive &&\
    dpkg --add-architecture arm64 &&\
    apt update &&\
    apt-get install -y--no-install-recommends libvips:arm64 clang:arm64 libclang-dev:arm64 libc6-dev:arm64
"""]

[target.arm-unknown-linux-gnueabihf]
pre-build = ["export DEBIAN_FRONTEND=noninteractive && dpkg --add-architecture $CROSS_DEB_ARCH && apt update && apt-get install -y libvips-dev clang libclang-dev libc6-dev-i386"]

[target.x86_64-unknown-linux-musl]
# pre-build = [
#   """
#     export DEBIAN_FRONTEND=noninteractive && \
#     apt update && \
#     apt install -y curl pip && \
#     pip install staticx && \
#     mkdir vips_build && \
#     curl -sL https://github.com/libvips/libvips/releases/download/v8.9.2/vips-8.9.2.tar.gz | tar -xz -f- --strip-components=1 -C ./vips_build && \
#     cd vips_build
#   """
# ]
pre-build = [
  """
    export DEBIAN_FRONTEND=noninteractive &&\
    apt update &&\
    apt-get install -y libvips-dev
  """
]
# pre-build = [
#   """
#     export DEBIAN_FRONTEND=noninteractive &&\
#     dpkg --add-architecture $CROSS_DEB_ARCH &&\
#     apt update &&\
#     apt-get install -y libvips-dev
#   """
# ]
# rustflags = [
#   "-C", "target-feature=-crt-static"
# ]
# pre-build = [ "apk --no-cache add vips vips-dev clang clang-dev build-base libc6-compat libwebp-dev && export CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_RUSTFLAGS==\"-C target-feature=-crt-static\" && export RUST_BACKTRACE=1" ]
